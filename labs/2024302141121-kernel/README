lab1 启动与串口输出
===================

学号: 2024302141121
协议: LAB1_BANNER_PROTOCOL=2
启动栈: LAB1_STACK_KB=12
节流周期: 16 + COURSE_SID % 16 = 17 个物理字节

本目录是 ~/projects/oslab 主仓库中的累积内核代码树，不单独初始化 Git。
lab1 已实现以下内容:

  - kernel/entry.S: M 态入口、12 KiB 栈、单核停驻和对齐陷阱向量
  - kernel/start.c: M→S 切换、delegation、PMP 和 Bare satp
  - kernel/console.c: 16550 轮询输出、个性化协议和节流
  - kernel/printf.c: 最小格式化输出
  - kernel/main.c: banner、printf 边界自检和协议 2 校验和
  - expect_banner.txt: QEMU 串口输出的逐字节期望值

构建与运行:

  make clean && make
  make qemu

退出 QEMU: 依次按 Ctrl-a、松开，再按 x。
设计、测试命令和实测记录见 ../../notes/lab1/。

以下文件为课程预置接口，lab1 实现过程中保持不变:

  Makefile
  kernel/kernel.ld
  kernel/riscv.h
  kernel/types.h
  kernel/memlayout.h
  kernel/course_sid.h
