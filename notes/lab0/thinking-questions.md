# lab0 思考题作答

> 不判分、不收缴，但 **lab1 验收问答直接从中抽选**（[学生须知 §三](../../docs/05-学生须知.md#三每轮实验的标准工作流)）。
> 用自己的话写，能画图就画图。背通用答案无用——问的是**因果关系**。

---

## 第一层（描述性）

### Q1. `fork` 返回后，父进程与子进程分别从哪一行继续执行？为什么两者的返回值不同？

_TODO_ — 提示：看 `fork()` 里 `np->trapframe->a0 = 0` 那一行，以及父进程 `return pid`。返回值走的是同一个 `a0`，但两份 trapframe。

### Q2. `exec` 加载新镜像时，原进程的虚拟地址空间发生了什么变化？

_TODO_ — 提示：`exec` 是先建新页表再切换，还是原地改？失败时怎么回滚？关注 `proc_pagetable` / `proc_freepagetable` 的调用顺序。

---

## 第二层（机制性）

### Q3. `fork` 复制页表时，为什么最高处的 trampoline 页必须映射为只读，而不需要重新分配物理页？

_TODO_ — 提示：这页里是什么内容？谁在什么特权级下执行它？所有进程共享一份会有问题吗？

### Q4. Shell 的 `read` 系统调用返回之前，Shell 进程处于什么状态？最终是由谁将其唤醒的？

_TODO_ — 提示：`consoleread` → `sleep(&cons.r, &cons.lock)`；唤醒方在中断上下文里。写出 chan 的具体值和唤醒者的调用栈。

### Q5. echo 发起的第一次 `write` 系统调用，底层是如何一步步转化为 UART 硬件寄存器写入的？

_TODO_ — 提示：`sys_write → filewrite → devsw[CONSOLE].write → consolewrite → uartputc → MMIO`。写出每一跳靠什么字段找到下一跳。

---

## 第三层（预测性）

### Q6. 如果内核启动代码中创建第一个用户进程的调用被注释掉，系统会停在何处？现象是什么？

_TODO_ — 提示：`main()` 里 `userinit()` 被注释 → `scheduler()` 会怎样？屏幕上能看到什么？`-d int` 日志里会有什么？

> 可以真的改一行试试，用 `-d int` / `info registers` 验证你的预测。**预测 → 实测 → 对账**是本课程的核心训练。

### Q7. 如果子进程 `exec` 完成且已退出，而父进程尚未调用 `wait`，此时进程表中子进程处于什么状态？

_TODO_ — 提示：`ZOMBIE`。它还占着哪些资源？哪些已经释放了？为什么必须留着？

### Q8. echo 执行完毕调用 `exit`，它打开的标准输出文件结构是如何保证不发生资源泄漏的？

_TODO_ — 提示：`exit` 里遍历 `ofile[]` 调 `fileclose`；`f->ref` 减到 0 才真正回收。sh 那一份引用为什么不受影响？

---

## 自己追加的问题

> 读代码时冒出来的、说明书没问的。这些往往是验收问答最出彩的地方。

- 💭 _TODO_
- 💭 _TODO_
