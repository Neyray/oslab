# AGENTS.md

本文件为 AI 辅助工具在本仓库中的工作约定。仓库总览见 [README.md](README.md)。

课程明确允许并鼓励使用 AI 辅助理解概念、编写代码与设计测试，无需提交对话记录（[学生须知 §五](docs/05-学生须知.md#五ai-辅助编程政策)）。本文件的作用是把课程的边界条件固化下来，使 AI 的输出方式与课程的考核方式一致。

## 项目背景

从零构建一个 RISC-V 内核的课程，15 周 / 7 轮实验 / 同一棵代码树累积演进。
本仓库是课程唯一主仓库；资料、笔记与个性化内核代码共享同一份 Git 历史。内核代码树位于 `labs/2024302141121-kernel/`，不得在该目录再次初始化独立仓库。

- 环境：WSL2 Ubuntu-22.04，ext4，路径 `~/projects/oslab`；课程要求避免置于 `/mnt/c/`
- 目标机：`qemu-system-riscv64 -machine virt -bios none`
- 工具链：`riscv64-unknown-elf-*`；**无 gdb 属于正常状态，不应建议安装或使用 gdb**

## 输出方式

课程验收问答围绕学生本人的个性化参数、自主选择的数据结构、自行编写的失败回滚路径展开；功能全部通过但无法解释因果关系者不予通过。因此：

- **机制解释优先于代码**。先说明设计成立的原因、边界所在、失败时的表现，再给实现。
- **结论必须可追溯到源码**。涉及参考树行为的断言，一律给出文件与行号，不得凭旧版 xv6 记忆或网络资料作答——本参考树与旧版差异很大（见 [notes/lab0/control-flow.md](notes/lab0/control-flow.md) 开头的差异表）。无法核实时明确说明是推导而非实测。
- 涉及设计决策（数据结构选型、不变式、回滚路径）时给出可选方案与权衡，判断权保留在学生一侧。
- 代码完成后提示对应的问答准备点：该段实现被抽问时的解释路径是什么。

关于 [notes/lab0/](notes/lab0/) 中已成稿的材料：正文由通读参考树后写成，数值来自 `memlayout.h` / `riscv.h` / `param.h` 常量与 `user/_echo` 的实际 ELF 布局。这些内容是**待本人复核的底稿**，不是终稿——`💭` 批注尤其需要本人验证并补充自己的观察，说明书要求的手绘图纸也必须本人绘制。后续修改应沿用同一标准：给出行号、区分实测与推导。

## 硬约束

- **不得修改系统预置文件**（引导代码、链接脚本、增量包内标注为预置的文件）。预置文件规定硬件与系统的接口边界。
- **不得从 MIT 公开 xv6 复制代码**。公开版是课程认可的原理阅读参考，但课程另有专属规范：定制系统调用号分配、用户程序内嵌加载机制、诊断输出规范、按学号派生的 15 项参数宏。复制代码无法通过课程测试与现场问答。
- **参数宏按学号派生（15 项）**。遇到 `LAB*_*` 形式的宏须查阅基线包中的实际定义，不得假设数值。
- 每轮验收包含早期实验的回归测试。改动前期模块时须提示回归风险。

## 版本控制

**不得执行 `git add` / `git commit` / `git tag` / `git push` / `git archive`。** 版本管理由学生本人操作。

验收现场会核实代码的作者归属，提交历史是直接证据，须由本人产生。AI 只修改工作树，完成后说明改动了哪些文件并可给出建议的提交信息，提交时机由本人决定。

只读的 git 命令（`git status` / `git diff` / `git log`，以及 `git bisect` 的诊断用途）可用于定位问题。

## 调试约定

不使用 gdb。三项核心手段按顺序尝试，完整说明见 [docs/07-无gdb调试手册.md](docs/07-无gdb调试手册.md)：

1. `-d int -D int.log` → `grep 'async:0' int.log | head` —— 读取第一个异常。`async:0` 为异常，`async:1` 为中断；lab2 起时钟中断频繁输出，须过滤后再看
2. `-d in_asm -D trace.log` → `tail -40` —— 最后一个基本块的最后一条指令即 CPU 停止前的动作
3. `objdump -d` + `addr2line -e kernel/kernel <epc 或 tval>` —— 由机器现场回溯到源码行
4. QEMU Monitor：`Ctrl-a c` 切入 → `info registers` / `info cpus` → `Ctrl-a c` 切回

需附加调试参数时不使用 `make qemu`，直接启动：

```bash
qemu-system-riscv64 -machine virt -bios none -kernel kernel/kernel -nographic \
    -d int -D int.log &
QPID=$!; sleep 5; kill $QPID
```

退出交互式 QEMU：`Ctrl-a` 松开后按 `x`。

cause 速查：`1` 取指访问错（lab1 多为 PMP 未配置或跳转至未映射地址）｜`2` 非法指令｜`5`/`7` 读写访问错（MMIO 基址错误）｜`8` U 态 ecall（正常）｜`12`/`13`/`15` 缺页（lab3 页表映射 / lab5 优先排查 COW 分支）｜`0x8000000000000005` S 态时钟中断（正常）

跨轮次回归定位：`git bisect start labN-submit labM-submit`。

## 文档约定

- 课程 docx 已转为 md 置于 [docs/](docs/)，原件保留在 [materials/](materials/)。仅修改 md，不修改原件；课程发布新版 docx 时重新转换而非手工改写。
- 笔记中的自主思考批注统一以 `> 💭` 开头。验收要求至少 3 处自主思考痕迹，该标记便于统计。
- 手绘图扫描件置于各 lab 目录的 `assets/`。
- 正文使用中文，寄存器名、函数名、代码标识符保持原文。

### design.md 编写标准

每轮 `notes/labN/design.md` 是现场问答索引，不是实现流水账。以当轮最新版实验说明书和增量包的接口约定为事实来源，并仿照 `notes/lab1/design.md` 组织：

1. 标题使用“labN 设计与验收问答”，开头说明覆盖的任务书版本、思考题与现场问答范围。
2. 给出可点击目录，正文至少依次包含：个性化参数速查、系统主线或阶段能力、任务书思考题完整回答、设计决策、现场短答、高频追问、故障现象与日志定位。
3. 任务书中的思考题必须保留原题和原编号逐题回答。每题先给一句可口述的结论，再沿“硬件或接口事实 → 软件动作 → 错误时的现象”展开；不得只写答案关键词。
4. 把任务书和增量包明确要求说明的数据结构、不变式、选型权衡、失败回滚路径写全。当前实现与任务书允许方案不同时，必须解释采用当前方案的环境依据和边界，不能伪装成任务书唯一答案。
5. 单独列出“现场三题速答”和“高频追问”。追问应覆盖个人参数、关键寄存器、汇编与 C 的接口、边界条件、错误路径、后续实验需重构之处，并给出可以直接口述的回答。
6. 控制流、状态机或中断链路适合用 Mermaid 或等宽文本图表示；图必须与当前源码一致，不能照搬公开 xv6 的旧版函数名。
7. 每个关键结论应能追溯到当前源码文件、函数或已核实的行号。引用行号前先确认代码已经稳定；后续代码改变导致行号漂移时同步更新。
8. 至少保留 3 处 `> 💭` 自主思考，内容应来自真实取舍、失败复现或不变式推导，不能写成装饰性评价。
9. 明确区分“本轮已经实现”“任务书允许但未采用”“后续实验才实现”。不得把最小过渡机制描述成完整调度、虚拟内存或文件系统。

### test-plan.md 编写标准

每轮 `notes/labN/test-plan.md` 是可直接照着执行的现场验收清单。以实验说明书的交付与验收顺序为主，补充官方测试、自写边界测试和早期实验回归：

1. 标题使用“labN 指令验收清单”，开头说明测试顺序与任务书、现场 Autograder 或官方测试的对应关系。
2. 按实际现场顺序编号，通常为：验收前状态核对、干净构建、早期实验回归、当轮启动现象、核心功能、错误路径、边界或压力测试、异常日志、预置文件完整性、提交标签与归档。
3. 每一项必须同时写出：可复制的完整命令、通过标准、当前实测结果。结果只能记录真实执行过的现象和数值；未执行项标为“待测”，不能推测为 PASS。
4. 命令从内核树或仓库根目录的明确 `cd` 开始，包含必要的超时、日志路径和退出处理，避免依赖读者已有终端状态。交互测试同时给出手工输入和自动化脚本入口。
5. 预期输出应给出稳定判据或正则，不把动态 PID、tick 等值硬编码；与个人参数相关的固定值必须写明推导来源。
6. 异常日志检查要区分正常 trap 与故障。例如 Lab2 的 U 态 ecall 是 `async:0 cause=8`，应过滤后统计其他同步异常，不能机械沿用 Lab1 的“所有 async:0 为 0”。
7. 至少包含两个自写测试：一个覆盖边界条件，一个覆盖错误或恢复路径；说明输入、预期不变式、失败表现和实际结果。
8. 回归测试必须覆盖说明书指定的早期实验能力，并尽量使用逐字节比较或确定性断言，而不是仅凭肉眼判断“看起来正常”。
9. 最后一节核对 `git status`、提交、`labN-submit` 标签和归档 CRC；是否已推送远端必须明确说明，不能把本地提交误写成远端提交成功。

## 单核与双核

基线为单核 `-smp 1`，单核即可评至 A 档（95 分）。lab4 起双核 `-smp 2` 通过可额外加 3 分，各轮独立选择、无连带影响。默认按单核基线实现，挑战双核需明确说明。
